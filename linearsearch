
#include <stdio.h>
int linearsearch(int a[], int n, int target)
{
    for (int i=0;i<n;i++)
    {
        if(a[i]==target)
        {
            return i;
        }
    }
   return -1; 
}
void main()
{
    int n,p,a[10],target;
    printf("enter the number of elements limit :");
    scanf("%d",&n);
    printf("enter the elements:");
    for (int i=0;i<n;i++)
    scanf("%d",&a[i]);
    printf("enter the target:");
    scanf("%d",&target);
    p=linearsearch(a,n,target);
    if(p!=-1)
    printf("element is present at position %d \n",p+1);
    else
    printf("elemet is not present");
}
